extends layout

block content
  h1= title

  - if (storyIndex)
    Your story has been saved! Send your friend to /#{storyIndex}!
  - else
    p Enter your story skeleton
    form(action='' method='POST')
      textarea(name='content')
      input(type='submit' value='Submit!')

